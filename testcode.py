if a in x:
   x[a] += 1
else:
   x[a] = 1

   
aa = {}
for app_meta in rr:
   id = app_meta[x']
   an = app_meta['id']
   if id in aa.keys():
      ua = aa[staffid]
      ua.append(an)
   else:
      aa[id] = [an]
